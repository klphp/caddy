# caddy

```shell

1、查看版本号
python3 -V 

2、安装依赖，注意python3.12-venv改为对应的版本号
sudo apt install -y git vim python3-pip -y

3、下载脚本
git clone https://github.com/klphp/caddy.git
cd caddy

4、安装依赖
pip install -r requirements.txt

5、修改main.py底部帐号密码后执行
python3 main.py
```